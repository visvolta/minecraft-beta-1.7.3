import {ITEM_TEXTURE_LIST}from '../assets/itemTextureList';const fallback='data:image/svg+xml,'+encodeURIComponent('<svg xmlns="http://www.w3.org/2000/svg" width="16" height="16"><rect width="16" height="16" fill="#f0f"/></svg>');
export class ItemIconResolver{private known=new Set(ITEM_TEXTURE_LIST);private cache=new Map<string,string>();static missing(){return fallback}resolve(id:string){const found=this.cache.get(id);if(found)return found;if(!this.known.has(id)){console.warn(`[presentation] missing required icon mapping: ${id}`);return fallback;}const url=`/textures/items/${id}.png`;this.cache.set(id,url);return url;}isKnown(id:string){return this.known.has(id)}}
