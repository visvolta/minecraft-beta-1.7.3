import * as THREE from 'three';import type{Inventory}from'../inventory/Inventory';import type{BlockRegistry}from'../blocks/BlockRegistry';import type{TextureAtlas}from'../assets/TextureAtlas';import type{ItemTextureAtlas}from'../assets/ItemTextureAtlas';import type{FirstPersonArmRenderer}from'./FirstPersonArmRenderer';import{presentationFor}from'../inventory/ItemRenderDefinition';import{IsolatedBlockModelBuilder}from'../inventory/IsolatedBlockModelBuilder';import{SpriteModelBuilder}from'../inventory/SpriteModelBuilder';
/** Stage 1 first-person held model owner; deliberately no third-person or dropped path. */export class FirstPersonHeldItemRenderer{readonly root=new THREE.Group();private mesh:THREE.Mesh|null=null;private key='';private blockMat:THREE.MeshBasicMaterial;private spriteMat:THREE.MeshBasicMaterial;constructor(arm:FirstPersonArmRenderer,private inv:Inventory,private blocks:BlockRegistry,private atlas:TextureAtlas,_items:ItemTextureAtlas){arm.armGroup.add(this.root);this.blockMat=new THREE.MeshBasicMaterial({map:atlas.texture,vertexColors:true,alphaTest:.1,transparent:true,fog:false});this.spriteMat=new THREE.MeshBasicMaterial({map:_items.texture,alphaTest:.1,transparent:true,side:THREE.DoubleSide,fog:false})}update(slot:number,_dt=0){const s=this.inv.getStack(slot),key=s?`${s.identity.type}:${s.identity.id}:${s.metadata}`:'';if(key===this.key)return this.mesh?.userData.block===true;this.key=key;if(this.mesh){this.root.remove(this.mesh);this.mesh.geometry.dispose();(this.mesh.userData.ownedMaterial as THREE.Material|undefined)?.dispose();(this.mesh.userData.ownedTexture as THREE.Texture|undefined)?.dispose();this.mesh=null}if(!s)return false;const d=presentationFor(s.identity,this.blocks);if(d.kind==='block'){const def=this.blocks.getById(s.identity.id as number);if(!def)return false;this.mesh=new THREE.Mesh(IsolatedBlockModelBuilder.build(def,this.atlas),this.blockMat);this.mesh.userData.block=true}else{const texture=new THREE.TextureLoader().load(`/textures/items/${String(s.identity.id)}.png`);texture.magFilter=THREE.NearestFilter;texture.minFilter=THREE.NearestFilter;texture.generateMipmaps=false;texture.colorSpace=THREE.SRGBColorSpace;const material=this.spriteMat.clone();material.map=texture;this.mesh=new THREE.Mesh(SpriteModelBuilder.build(0,0,1,1),material);this.mesh.userData.block=false;this.mesh.userData.ownedTexture=texture;this.mesh.userData.ownedMaterial=material}this.mesh.position.set(...d.firstPerson.position);this.mesh.rotation.set(...d.firstPerson.rotation);this.mesh.scale.setScalar(d.firstPerson.scale);this.root.add(this.mesh);return this.mesh.userData.block===true}dispose(){this.mesh?.geometry.dispose();this.blockMat.dispose();this.spriteMat.dispose()}}
