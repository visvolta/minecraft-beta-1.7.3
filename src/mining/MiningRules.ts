import type { BlockDefinition } from '../blocks/BlockDefinition';import { DEFAULT_ITEM_DEFINITIONS,type ItemDefinitionRegistry } from '../items/ItemDefinitionRegistry';import type { ItemStack } from '../inventory/ItemStack';
export interface MiningContext{readonly block:BlockDefinition;readonly held:ItemStack|null;readonly underwater:boolean;readonly grounded:boolean;}
export interface MiningResult{readonly progressPerTick:number;readonly canHarvest:boolean;readonly strength:number;readonly instant:boolean;readonly unbreakable:boolean;}
export function calculateMining(context:MiningContext,items:ItemDefinitionRegistry=DEFAULT_ITEM_DEFINITIONS):MiningResult{const {block}=context,hardness=block.hardness??1;if(hardness<0||block.unbreakable)return{progressPerTick:0,canHarvest:false,strength:0,instant:false,unbreakable:true};const tool=context.held?.identity.type==='item'?items.get(context.held.identity.id):undefined;const canHarvest=!block.requiresCorrectToolForDrops||(tool!==undefined&&tool.toolClass===block.harvestToolClass&&(tool.harvestLevel??-1)>=(block.minimumHarvestLevel??0));if(hardness===0||block.instantBreak)return{progressPerTick:1,canHarvest,strength:1,instant:true,unbreakable:false};let strength=1;if(tool?.toolClass==='sword')strength=1.5;else if(tool!==undefined&&tool.toolClass===block.preferredToolClass)strength=tool.miningSpeed??1;if(context.underwater)strength/=5;if(!context.grounded)strength/=5;const progressPerTick=canHarvest?strength/hardness/30:1/hardness/100;return{progressPerTick,canHarvest,strength,instant:false,unbreakable:false};}
export function crackStage(progress:number):number{return Math.max(0,Math.min(9,Math.floor(progress*10)));}
