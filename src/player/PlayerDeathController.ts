import type { Inventory } from '../inventory/Inventory';import type { ItemEntityManager } from '../entities/items/ItemEntityManager';import type { JavaRandom } from '../world/generation/random/JavaRandom';import type { Player } from './Player';import type { DeathScreen } from './DeathScreen';
export class PlayerDeathController{private processed=false;public readonly deathLocation={x:0,y:0,z:0};
 public constructor(private readonly player:Player,private readonly inventory:Inventory,private readonly items:ItemEntityManager,private readonly rng:JavaRandom,private readonly screen:DeathScreen,private readonly onProcessed?:()=>void){}
 public update():void{if(this.player.isAlive()){this.processed=false;return;}if(this.processed)return;this.processed=true;this.deathLocation.x=this.player.position.x;this.deathLocation.y=this.player.position.y;this.deathLocation.z=this.player.position.z;for(let i=0;i<this.inventory.getSlots().length;i++){const stack=this.inventory.getStack(i);if(!stack)continue;this.inventory.setStack(i,null);const angle=this.rng.nextFloat()*Math.PI*2,speed=.1+this.rng.nextFloat()*.2;this.items.spawnThrownItem(this.player.position.x,this.player.position.y+.5,this.player.position.z,{type:stack.identity.type,id:stack.identity.id,count:stack.count,metadata:stack.metadata},Math.cos(angle)*speed,.2+this.rng.nextFloat()*.15,Math.sin(angle)*speed,40);}this.player.wishVelocity.x=this.player.wishVelocity.z=0;this.screen.open();this.onProcessed?.();}
 public reset():void{this.processed=false;}}
