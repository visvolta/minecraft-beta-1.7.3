import type { HotbarHudRenderer } from '../inventory/HotbarHudRenderer';import type { Player } from './Player';import { HealthHudRenderer } from './HealthHudRenderer';import { HungerHudRenderer } from './HungerHudRenderer';
export class HudRenderer{public readonly health:HealthHudRenderer;public readonly hunger:HungerHudRenderer;public constructor(public readonly hotbar:HotbarHudRenderer,player:Player){this.health=new HealthHudRenderer(player);this.hunger=new HungerHudRenderer(player);}public update(selected:number):void{this.hotbar.update(selected);const layout=this.hotbar.getLayout();this.health.update(layout);this.hunger.update(layout);}public render():void{this.hotbar.render();}public dispose():void{this.health.dispose();this.hunger.dispose();this.hotbar.dispose();}}
